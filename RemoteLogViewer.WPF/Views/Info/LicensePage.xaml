<UserControl
    x:Class="RemoteLogViewer.WPF.Views.Info.LicensePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:RemoteLogViewer.WPF.Views.Info"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:RemoteLogViewer.Core.ViewModels.Info;assembly=RemoteLogViewer.Core"
    mc:Ignorable="d">

    <Grid>
        <ItemsControl ItemsSource="{Binding Licenses}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
						<TextBlock TextWrapping="Wrap">
							<Run Text="{Binding Name}" FontWeight="SemiBold"/>
							<Run Text="{Binding License}" FontFamily="Consolas" Foreground="DarkGray"/>
						</TextBlock>
						<TextBlock>
							<Hyperlink Click="Hyperlink_Click" >
								<TextBlock Text="{Binding Url}" TextWrapping="Wrap"/>
							</Hyperlink>
						</TextBlock>
					</StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
